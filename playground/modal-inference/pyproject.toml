[project]
name = "modal-inference"
version = "0.1.0"
description = "Modal inference wrappers for the Copilot Hackathon playground video-analysis experiments."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "modal>=1.1.0",
]

[tool.uv]
python-preference = "only-system"
